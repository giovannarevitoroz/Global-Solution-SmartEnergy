pip install flask
pip install flask_cors
pip install joblib
pip install pandas
pip install scikit-learn==1.5.2 --> caso haja algum aviso de problema de versão, caso contrário usar pip install scikit-learn

python api.py --> inicia API (VSCode)